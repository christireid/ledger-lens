#!/usr/bin/env node
console.error("scripts/db-migrate.mjs: not implemented yet — this lands with its owning milestone (see PROGRESS.md). Failing loudly rather than pretending success (§27.2e).");
process.exit(1);
